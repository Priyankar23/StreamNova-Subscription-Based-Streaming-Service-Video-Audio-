spring.application.name=SubscriptionBasedStreamingService

spring.datasource.url=jdbc:mysql://localhost:3306/streaming
spring.datasource.username=root
spring.datasource.password=Priya@123
spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

server.port=8082
# JWT Configuration
jwt.secret=mySuperSecretKey12345!
jwt.expiration=86400000  # 24 hours expiration time
jwt.header=Authorization  # The HTTP header key to expect the token in
jwt.prefix=Bearer  # The token prefix (e.g., 'Bearer <JWT>')
spring.resources.static-locations=classpath:/static/, file:uploads/
spring.servlet.multipart.enabled=true


spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
